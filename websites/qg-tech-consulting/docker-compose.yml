version: '3.8'

services:
  astro:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "4321:4321"
    command: sh -c "npm install && npm run dev -- --host"
    stdin_open: true
    tty: true
